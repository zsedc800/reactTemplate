import React from 'react'
import { RouterView } from '@/router'
class App extends React.Component {
  constructor (props) {
    super(props)
  }

  render () {
    return (
      <RouterView/>
    )
  }
}

export default App
